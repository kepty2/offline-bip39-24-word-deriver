export default function Home() {
  // The Worker serves the standalone HTML asset at /. This route remains
  // intentionally empty because the wallet interface is the auditable file in
  // public/offline-bip39-24-word-deriver.html.
  return null;
}
